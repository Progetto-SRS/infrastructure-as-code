name: Deploy test environment
 
on: 
  push:
    branches:
    - main
  workflow_dispatch:
    
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:

      # Checkout code
    - name: Checkout code
      uses: actions/checkout@main

      # Log into Azure
    - name: Login into Azure
      uses: azure/CLI@v1
      with:
        azcliversion: 2.30.0
        inlineScript: az login -u ${{ secrets.AZURE_USERNAME }} -p ${{ secrets.AZURE_PWD }}

      # Deploy ARM template
    - name: Deploy ARM template
      uses: azure/CLI@v1
      with:
        azcliversion: 2.30.0
        inlineScript: az deployment group create --resource-group ${{ secrets.AZURE_TEST_ENV }} --template-file ./template-test-env.json

      # output containerName variable from template
    - run: echo ${{ steps.deploy.outputs.containerName }}
